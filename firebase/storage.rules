rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Fail closed until tenant branding and its membership rules are implemented.
    match /{path=**} {
      allow read, write: if false;
    }
  }
}
